cmake_minimum_required(VERSION 3.5)
project(proy_fireye_ros2)

# Solo necesitamos la herramienta de construcción de ROS 2
find_package(ament_cmake REQUIRED)

# Esta función registra el paquete en el sistema
ament_package()